# Generic iPad Native Shell

Temporary, source-free build container for an unsigned Capacitor iPad shell.

This repository contains no Trading Platform engine, strategy, UI source, compiled web bundle, market data, credentials, or private repository history. The workflow builds only the generic native wrapper. Private web assets are inserted and verified locally before local signing.
